package testClasses;

/**
 * This class lists the manual tests for the Student Post GUI.
 * 
 * Manual tests are tests that a person does by clicking through the program.
 * These tests check if the student can use the post screen correctly.
 */
public class StudentPostManualTests {

    /**
     * Manual Test 1: Create a post.
     * 
     * Steps:
     * 1. Open the program.
     * 2. Log in as a student.
     * 3. Go to the student post page.
     * 4. Type a valid post.
     * 5. Click the button to create the post.
     * 
     * Expected result:
     * The post is created and shown on the screen.
     */
    public void manualTestCreatePost() {
        // This is a manual test, so there is no code here.
    }

    /**
     * Manual Test 2: Try to create a post with no content.
     * 
     * Steps:
     * 1. Open the student post page.
     * 2. Leave the post content blank.
     * 3. Click the button to create the post.
     * 
     * Expected result:
     * The program should show an error message.
     * The empty post should not be created.
     */
    public void manualTestEmptyPost() {
        // This is a manual test, so there is no code here.
    }

    /**
     * Manual Test 3: Delete a post.
     * 
     * Steps:
     * 1. Open the student post page.
     * 2. Select a post.
     * 3. Click the delete button.
     * 
     * Expected result:
     * The post should be marked as deleted or removed from the list.
     */
    public void manualTestDeletePost() {
        // This is a manual test, so there is no code here.
    }

    /**
     * Manual Test 4: Mark a post as read.
     * 
     * Steps:
     * 1. Open the student post page.
     * 2. Select or open a post.
     * 
     * Expected result:
     * The program should mark the post as read for that user.
     */
    public void manualTestMarkPostAsRead() {
        // This is a manual test, so there is no code here.
    }
}